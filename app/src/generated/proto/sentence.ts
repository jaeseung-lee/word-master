// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.2
// source: sentence.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "./google/protobuf/timestamp";
import { Language, languageFromJSON, languageToJSON } from "./language";
import { Word } from "./word";

export const protobufPackage = "sentence";

export interface Sentence {
  id: number;
  text: string;
  definition: string;
  language: Language;
  isBookmarked: boolean;
  createTime: Date | undefined;
  updateTime: Date | undefined;
}

export interface SentenceComposite {
  sentence: Sentence | undefined;
  wordList: Word[];
}

function createBaseSentence(): Sentence {
  return {
    id: 0,
    text: "",
    definition: "",
    language: 0,
    isBookmarked: false,
    createTime: undefined,
    updateTime: undefined,
  };
}

export const Sentence: MessageFns<Sentence> = {
  encode(
    message: Sentence,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    if (message.definition !== "") {
      writer.uint32(26).string(message.definition);
    }
    if (message.language !== 0) {
      writer.uint32(32).int32(message.language);
    }
    if (message.isBookmarked !== false) {
      writer.uint32(40).bool(message.isBookmarked);
    }
    if (message.createTime !== undefined) {
      Timestamp.encode(
        toTimestamp(message.createTime),
        writer.uint32(50).fork(),
      ).join();
    }
    if (message.updateTime !== undefined) {
      Timestamp.encode(
        toTimestamp(message.updateTime),
        writer.uint32(58).fork(),
      ).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Sentence {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSentence();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.definition = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.language = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isBookmarked = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.createTime = fromTimestamp(
            Timestamp.decode(reader, reader.uint32()),
          );
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.updateTime = fromTimestamp(
            Timestamp.decode(reader, reader.uint32()),
          );
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Sentence {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      definition: isSet(object.definition)
        ? globalThis.String(object.definition)
        : "",
      language: isSet(object.language) ? languageFromJSON(object.language) : 0,
      isBookmarked: isSet(object.isBookmarked)
        ? globalThis.Boolean(object.isBookmarked)
        : isSet(object.is_bookmarked)
          ? globalThis.Boolean(object.is_bookmarked)
          : false,
      createTime: isSet(object.createTime)
        ? fromJsonTimestamp(object.createTime)
        : isSet(object.create_time)
          ? fromJsonTimestamp(object.create_time)
          : undefined,
      updateTime: isSet(object.updateTime)
        ? fromJsonTimestamp(object.updateTime)
        : isSet(object.update_time)
          ? fromJsonTimestamp(object.update_time)
          : undefined,
    };
  },

  toJSON(message: Sentence): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.definition !== "") {
      obj.definition = message.definition;
    }
    if (message.language !== 0) {
      obj.language = languageToJSON(message.language);
    }
    if (message.isBookmarked !== false) {
      obj.isBookmarked = message.isBookmarked;
    }
    if (message.createTime !== undefined) {
      obj.createTime = message.createTime.toISOString();
    }
    if (message.updateTime !== undefined) {
      obj.updateTime = message.updateTime.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Sentence>, I>>(base?: I): Sentence {
    return Sentence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Sentence>, I>>(object: I): Sentence {
    const message = createBaseSentence();
    message.id = object.id ?? 0;
    message.text = object.text ?? "";
    message.definition = object.definition ?? "";
    message.language = object.language ?? 0;
    message.isBookmarked = object.isBookmarked ?? false;
    message.createTime = object.createTime ?? undefined;
    message.updateTime = object.updateTime ?? undefined;
    return message;
  },
};

function createBaseSentenceComposite(): SentenceComposite {
  return { sentence: undefined, wordList: [] };
}

export const SentenceComposite: MessageFns<SentenceComposite> = {
  encode(
    message: SentenceComposite,
    writer: BinaryWriter = new BinaryWriter(),
  ): BinaryWriter {
    if (message.sentence !== undefined) {
      Sentence.encode(message.sentence, writer.uint32(10).fork()).join();
    }
    for (const v of message.wordList) {
      Word.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SentenceComposite {
    const reader =
      input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSentenceComposite();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sentence = Sentence.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.wordList.push(Word.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SentenceComposite {
    return {
      sentence: isSet(object.sentence)
        ? Sentence.fromJSON(object.sentence)
        : undefined,
      wordList: globalThis.Array.isArray(object?.wordList)
        ? object.wordList.map((e: any) => Word.fromJSON(e))
        : globalThis.Array.isArray(object?.word_list)
          ? object.word_list.map((e: any) => Word.fromJSON(e))
          : [],
    };
  },

  toJSON(message: SentenceComposite): unknown {
    const obj: any = {};
    if (message.sentence !== undefined) {
      obj.sentence = Sentence.toJSON(message.sentence);
    }
    if (message.wordList?.length) {
      obj.wordList = message.wordList.map((e) => Word.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SentenceComposite>, I>>(
    base?: I,
  ): SentenceComposite {
    return SentenceComposite.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SentenceComposite>, I>>(
    object: I,
  ): SentenceComposite {
    const message = createBaseSentenceComposite();
    message.sentence =
      object.sentence !== undefined && object.sentence !== null
        ? Sentence.fromPartial(object.sentence)
        : undefined;
    message.wordList = object.wordList?.map((e) => Word.fromPartial(e)) || [];
    return message;
  },
};

type Builtin =
  | Date
  | Function
  | Uint8Array
  | string
  | number
  | boolean
  | undefined;

export type DeepPartial<T> = T extends Builtin
  ? T
  : T extends globalThis.Array<infer U>
    ? globalThis.Array<DeepPartial<U>>
    : T extends ReadonlyArray<infer U>
      ? ReadonlyArray<DeepPartial<U>>
      : T extends {}
        ? { [K in keyof T]?: DeepPartial<T[K]> }
        : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin
  ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & {
      [K in Exclude<keyof I, KeysOfUnion<P>>]: never;
    };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
